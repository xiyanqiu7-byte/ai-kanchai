# 怎么把代码弄到你电脑（小白版）

## 先说清楚

Cloud Agent **网页上一般看不到**我打包的 `zip`，这是正常的，不是你没找对。  
我也 **不能直接拖文件进你的电脑**。  
现在最稳的办法：把代码推进你的 GitHub，再在你电脑下载。

仓库：https://github.com/xiyanqiu7-byte/ai-kanchai

---

## 方案 A（推荐）：让我有权限推到 GitHub

### 做法 1：在 Cursor 里连接 GitHub
1. 打开 Cursor 设置里的 **GitHub / 账户 / Integrations**（名称可能略有不同）
2. 登录并授权访问你的仓库 `xiyanqiu7-byte/ai-kanchai`
3. 回到这个对话，回复一句：**「GitHub 已授权，请推送」**

我收到后就会把代码推到仓库，你再按下面「下载到电脑」做。

### 做法 2：给我一个一次性 Token（也能推）
1. 打开：https://github.com/settings/tokens  
2. 生成 **Fine-grained token** 或 classic token  
   - 权限只要能写这个仓库（`repo` / Contents: Read and write）
3. 在对话里发我（用完建议马上删除/作废）
4. 我说「推送成功」后，你去仓库刷新能看到文件

---

## 方案 B：你电脑用 Cursor 本地重建（不依赖网页下载）

1. 电脑安装 Cursor：https://cursor.com  
2. 菜单：**File → Open Folder**，新建一个空文件夹，例如 `D:\ai-kanchai`  
3. 在这个文件夹里开 Cursor Chat / Agent，把下面整段发给它：

```text
请在当前文件夹从零搭建 AI砍柴 桌面 MVP：
- Electron + React + Vite + TypeScript
- Python FastAPI 分析服务（切镜 + OpenCV YuNet/SFace 人脸匹配，模型缺失时自动下载）
- 交互参考 LosslessCut：拖拽视频、时间轴、I/O、右侧片段列表、快捷键
- 角色从当前帧点选人脸建档
- 导出 mp4，文件名模板：{集数}-{镜号}{日/夜}·{造型}·{简述}.mp4
- 写好 README 和 Windows 小白安装步骤
参考仓库：https://github.com/xiyanqiu7-byte/ai-kanchai（目前可能还是空的）
以及这次云端对话里的需求说明。
```

本地 Agent 会在你电脑上直接生成，生成完再按 README 运行。

---

## 代码进仓库后：下载到电脑

任选一种：

### 用网页
1. 打开 https://github.com/xiyanqiu7-byte/ai-kanchai  
2. 绿色 **Code** 按钮 → **Download ZIP**  
3. 解压

### 用命令（装了 Git）
```text
git clone https://github.com/xiyanqiu7-byte/ai-kanchai.git
cd ai-kanchai
```

然后看 `本地安装-小白版.md` 安装 Node / Python / ffmpeg，再运行：

```text
npm install
python -m pip install -r analyzer\requirements.txt
npm run dev
```
