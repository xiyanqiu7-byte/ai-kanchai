# AI砍柴 · 小白本地安装（Windows）

按顺序做，做完一步再做下一步。

## 一、先装这 3 个软件（只需装一次）

### 1）Node.js
1. 打开：https://nodejs.org/
2. 下载 **LTS** 版本，一路下一步安装
3. 装完后打开「命令提示符」或 PowerShell，输入：
```text
node -v
```
能看到版本号就对了。

### 2）Python
1. 打开：https://www.python.org/downloads/
2. 下载并安装
3. **很重要**：安装时勾选 `Add python.exe to PATH`
4. 验证：
```text
python --version
```

### 3）ffmpeg
1. 打开：https://www.gyan.dev/ffmpeg/builds/
2. 下载 `ffmpeg-release-essentials.zip`
3. 解压到例如 `C:\ffmpeg`
4. 把 `C:\ffmpeg\bin` 加到系统环境变量 Path
5. 验证：
```text
ffmpeg -version
```

> 如果环境变量不会配，也可以先装 [winget](https://learn.microsoft.com/zh-cn/windows/package-manager/winget/) 后执行：
> `winget install Gyan.FFmpeg` / `winget install OpenJS.NodeJS.LTS` / `winget install Python.Python.3.12`

---

## 二、拿到项目文件夹

打开仓库：https://github.com/xiyanqiu7-byte/ai-kanchai

如果仓库里已经有代码：

1. 点绿色 **Code** → **Download ZIP**
2. 解压，进入里面的项目文件夹（能看到 `package.json`）

如果仓库还是空的：先看同目录的 `怎么弄到电脑.md`，让我把代码推上去。

---

## 三、安装依赖并启动

在项目文件夹里打开终端（Shift + 右键 → “在此处打开 PowerShell”），依次执行：

```text
npm install
python -m pip install -r analyzer\requirements.txt
npm run dev
```

稍等一会儿，会弹出 **AI砍柴** 窗口。

---

## 四、怎么试用

1. 拖入一部成片（或点左上角「打开视频」）
2. 播到角色正脸，按键盘 **E**，点选人脸建档，填造型名（如「粗布麻衣」）
3. 按 **A** 开始分析，右侧会出现候选片段
4. 逐段确认：排除误检、填「日/夜」和剧情简述
5. 点右下角导出 MP4

常用快捷键：
- 空格：播放/暂停
- J / L：减速 / 加速
- `,` / `.`：上一帧 / 下一帧
- I / O：设入点 / 出点

---

## 五、常见问题

**窗口没出来 / 报错找不到 python**  
- 重开终端再试
- 确认 `python --version` 有输出

**分析失败 / 导不出**  
- 确认 `ffmpeg -version` 有输出

**拖文件没反应**  
- 改用左上角「打开视频」

卡住了把终端里的红字整段复制发给我即可。
